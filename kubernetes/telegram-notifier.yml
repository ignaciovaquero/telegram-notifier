apiVersion: v1
kind: Service
metadata:
  name: telegram-notifier
spec:
  selector:
    app: telegram-notifier
  type: ClusterIP
  ports:
  - name: http
    port: 8081
    targetPort: http
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: telegram-notifier
  labels:
    app: telegram-notifier
spec:
  selector:
    matchLabels:
      app: telegram-notifier
  replicas: 2
  template:
    metadata:
      labels:
        app: telegram-notifier
    spec:
      imagePullSecrets:
        - name: regcred
      containers:
        - image: registry.ivaquero.es/telegram-notifier:1.0.0
          name: telegram-notifier
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 200m
              memory: 256Mi
          env:
            - name: NOTIFIER_BOT_TOKEN
              valueFrom:
                secretKeyRef:
                  name: telegram
                  key: token
          ports:
            - containerPort: 8081
              name: http
---
apiVersion: autoscaling/v1
kind: HorizontalPodAutoscaler
metadata:
  name: telegram-notifier
  labels:
    app: telegram-notifier
spec:
  maxReplicas: 5
  minReplicas: 2
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: telegram-notifier
  targetCPUUtilizationPercentage: 70
